name: "Deploy para VPS"
on:
  push:
    branches:
      - main
  workflow_dispatch: 

jobs: 
  build_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Deploy with ssh
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOSTINGER_REMOTE_SERVER_IP }}
          username: root
          passphrase: ${{ secrets.HOSTINGER_SSH_PASSPHRASE }}
          key: ${{ secrets.HOSTINGER_GITHUB_SSH_KEY }}
          script: |
            cd /home/evento_planejado_api/eventoplanejadoserver/
            npm install
            npm run build
            pm2 restart api_evento_planejado_server
        